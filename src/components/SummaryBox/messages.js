import { defineMessages } from 'react-intl';

export default defineMessages({
  dateInterval: {
    id: 'app.components.SummaryBox.dateInterval',
    defaultMessage: '{startDate} - {endDate}',
  },
});